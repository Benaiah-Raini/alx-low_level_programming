#include <stdio.h>
/**
 * main - main function printf
 * Return: 0
 */
int main(void)
{ /* declare a variable and initiate it */
char alpha = 'a';
/* while loop */
while (alpha <= 'z')
{
putchar(alpha);
alpha++;
}
putchar('\n');
return (0);
}
