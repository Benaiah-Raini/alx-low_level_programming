#include "main.h"

/**
 * mul - this is the function that adds two digits
 * @a: The character to be multiplied
 * @b: The character to be multiplied
 * Return: the multiplication of two digits
 */
int mul(int a, int b)
{
return (a * b);
}
